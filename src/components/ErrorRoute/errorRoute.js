import React from "react";

const ErrorRoute = props => {
  return (
    <main className="main-section">
      Sorry we encountered an error.... It seems that the path you entered
      doesn't exist.
    </main>
  );
};

export default ErrorRoute;
